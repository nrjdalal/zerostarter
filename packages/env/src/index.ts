export { getSafeEnv } from "@/lib/safe-env"
export {
  VERSION,
  GIT_SHA,
  BUILD_VERSION,
  getBuildVersion,
  NODE_ENV,
  isLocal,
  isDevelopment,
  isTest,
  isStaging,
  isProduction,
  type NodeEnv,
} from "@/lib/constants"
