{
  "name": "@tests/app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check-types": "tsc --noEmit",
    "screenshots": "bun scripts/screenshots.ts",
    "test": "bun test --timeout 60000",
    "test:api": "bun test --timeout 60000 api",
    "test:web": "bun test --timeout 60000 web"
  },
  "devDependencies": {
    "@packages/config": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
