# More deployment adapters, methods and platforms

- Status: backlog
- Links: #154

Prove out additional deploy targets on throwaway branches (e.g. `playground-railway`, `playground-cloudflare`), then open a PR per adapter once one works.
